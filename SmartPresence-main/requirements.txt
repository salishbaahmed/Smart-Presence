# ── Vision & AI ──
opencv-python>=4.5.5       # Camera capture & image processing
mediapipe>=0.8.9           # Face mesh for blink-based liveness detection
face_recognition>=1.3.0    # Face encoding & matching (wraps dlib)
dlib>=19.22.0              # Face detection engine (needs Visual Studio C++ Build Tools)
numpy>=1.21.0              # Matrix operations for encoding comparisons

# ── Web Backend ──
Flask>=2.0.1               # Web server & routing
Werkzeug>=2.0.1            # Password hashing & HTTP utilities

# ── Data & Reporting ──
openpyxl>=3.0.7            # Excel file export (.xlsx)

# ── Config & HTTP ──
python-dotenv>=0.19.0      # Load .env file configuration
requests>=2.26.0           # Telegram API for crash reports

# ── Security ──
Flask-WTF>=1.2.0           # CSRF protection
Flask-Limiter>=3.0.0       # Rate limiting for login/PIN endpoints
